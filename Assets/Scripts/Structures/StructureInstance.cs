using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StructureInstance : MonoBehaviour, IHealth
{
    // Start is called before the first frame update
    public int Health {get; set;}
    
}
